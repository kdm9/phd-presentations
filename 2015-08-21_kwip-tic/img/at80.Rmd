---
title: "at80"
output: pdf_document
---

load it in

```{r}
dst = read.delim("at80.dist")
dstmat = as.matrix(dst[,-1])
dst = as.dist(dstmat)
meta = read.delim("at80_meta.tab")
stats = read.delim("at80-stats.tab")
```

dendro time

```{r}
hcl = hclust(dst, method="complete")
plot(hcl, cex=0.6)
```
